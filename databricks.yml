bundle:
  name: case_ifood_nyctaxis
  uuid: 997902b0-ea69-40f2-8d65-261a46ec477d

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: link_host

  prod:
    mode: production
    workspace:
      host: link_host
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: seu_email@email.com
        level: CAN_MANAGE
